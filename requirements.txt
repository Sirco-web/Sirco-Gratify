# Gratify LLM - Requirements

# Core dependencies
numpy>=1.24.0
torch>=2.0.0  # Will be upgraded for GPU in setup if needed
torchaudio>=2.0.0

# Optional GPU support (uncomment as needed):
# For NVIDIA CUDA: torch==2.1.1+cu121 (install via installation script)
# For AMD ROCm: torch==2.1.1+rocm5.7 
# For Apple Metal: torch==2.1.1

# Development and utilities
colorama>=0.4.6
tqdm>=4.65.0

# Notebook support (optional)
jupyter>=1.0.0
ipython>=8.0.0
